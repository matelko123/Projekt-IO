<Window x:Name="Window" x:Class="Projekt_wlasciwy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt_wlasciwy"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="900" ResizeMode="CanMinimize">

    <Window.Background>

        <ImageBrush ImageSource="tło.jpg"></ImageBrush>

    </Window.Background>
    
       

    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <StackPanel x:Name="LeftPanel" Grid.Column="0">

            <Image  Height="100" VerticalAlignment="Top" Source="Logo.png"/>
            <Button Height="50" Content="File Sort"/>
            <Button Height="50" Content="Pictures Compressor"/>


        </StackPanel>

        <DockPanel Background="AliceBlue" Height="30" VerticalAlignment="Bottom">

            <Label HorizontalAlignment="Left" VerticalAlignment="Center" Content="Mode"/>
            <ToggleButton x:Name="Mode" Height="20" Width="50" Content="Light" Checked="ToggleButton_Checked" />
        </DockPanel>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Visible" CanContentScroll="True">
        <StackPanel Name="Wrapper" Grid.Column="1" Margin="0,0,0,35" >
                <StackPanel Name="WindowsComponents" Grid.Column="1" Margin="0">
                    <local:PathWindow ></local:PathWindow>
                </StackPanel>
            
            <Button Name="Plus" Opacity="0.6" Width="300" Content="+" FontSize="20" MouseEnter="Plus_MouseEnter" MouseLeave="Plus_MouseLeave" Click="Plus_Click" />
            </StackPanel>
       </ScrollViewer>
        <DockPanel Height="30" VerticalAlignment="Bottom" Grid.Column="1">

            <DockPanel.Background>
                <ImageBrush Stretch="Fill"  ImageSource="grey.jpg" ></ImageBrush>
            </DockPanel.Background>
            <Label Content="Progress:" Height="30" Width="100" VerticalAlignment="Center"/>
            <ProgressBar Height="10" Width="450"/>
            
            
        </DockPanel>

        

       
    </Grid>
</Window>
